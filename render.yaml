services:
  - type: web
    name: hdr-studio-processor
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 8000
    healthCheckPath: /health
